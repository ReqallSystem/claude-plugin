{
  "mcpServers": {
    "reqall": {
      "type": "http",
      "url": "${REQALL_URL:-https://www.reqall.net}/mcp",
      "headers": {
        "Authorization": "Bearer ${REQALL_API_KEY}"
      }
    }
  }
}
