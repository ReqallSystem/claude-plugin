{
  "name": "@reqall/claude-plugin",
  "version": "2026.9.2",
  "description": "Reqall plugin for Claude Code — persistent semantic memory for AI agents",
  "type": "module",
  "main": "dist/src/index.js",
  "bin": {
    "reqall-claude-plugin": "dist/src/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test tests/hooks.test.mjs",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  },
  "files": [
    "dist",
    "hooks/hooks.json",
    "skills",
    "agents",
    ".claude-plugin",
    ".mcp.json"
  ],
  "license": "MIT"
}
